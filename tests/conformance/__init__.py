"""Regression tests pinning findings from the spec-conformance audits.

These cut across packages on purpose: each test names the clause of the
XCTrack Competition Interfaces spec it holds the library to, whichever
modules that ends up exercising.
"""
